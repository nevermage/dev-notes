import { defineConfig } from 'drizzle-kit';

export default defineConfig({
    schema: './src/notes/db/schema.ts',
    out: './drizzle',
    dialect: 'postgresql',
    dbCredentials: {
        url: 'postgresql://USER:PASSWORD@localhost:5433/devnotes_search',
    },

    strict: true,
    verbose: true,
});
